# Render Utils Overview

`src/rtr/system/render/utils/` contains small helper utilities used by render pipelines but not owned by the core renderer bootstrap.

At the moment it contains shader compilation support for editable ShaderToy-style workflows.
